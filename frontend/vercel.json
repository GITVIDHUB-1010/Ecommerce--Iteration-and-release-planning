{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-builds",
      "config": { "distDir": "build" }
    }
  ],
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
